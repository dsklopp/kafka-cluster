---
driver:
  name: vagrant
  customize:
    cpus: 1
    memory: 1024


provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-14.04

suites:
  - name: default
    driver:
      network:
        - ["private_network", {ip: "192.168.17.201"}]
    run_list:
      recipe[kafka-cluster::default]
    attributes:
